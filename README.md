# PhotoCherryPick
PhotoCherryPick (PCP) automatically extracts the best frames from videos, scene by scene. Using local ML models, it detects faces, open eyes, and smiles while discarding blurry shots. It also injects original video metadata (EXIF) into the extracted photos. 100% offline.
